---
// src/components/Cover.astro
import Intro from "@components/Intro.astro";
import Hi from "@components/Hi.astro";
---

<style>
    .brand {
        font-weight: 200 !important;
        font-size: 200%;
        line-height: 1;
        padding-block: 0;
    }
    .cover {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: var(--spaceI) var(--space_);
        min-height: 100svh;
        height: 100svh;
        background-size: cover;
        background-color: #0069cc;
        color: var(--white);
    }
    .slogan {
        padding-top: calc(var(--spaceI) * 2);
        padding-bottom: calc(var(--spaceI) / 4);
        padding-inline: calc(var(--spaceI) * 1);
        text-wrap: pretty;
        text-align: center;
    }
</style>

<header class="cover js_scrollto01">
    <Hi />
    <Intro />

    <div class="slogan" id="slogan0">
        <p>Making life easier.</p>
        <p>For you and your customers.</p>
        <h1 class="brand">JUNGLESTAR</h1>
        <p>We can help.</p>
    </div>
</header>
