---
interface Props {
  link: string;
  title?: string;
  text: string;
  linkClass?: string;
}

const {
  link = "/",
  title = "back home",
  text = "CTAbutton",
  linkClass,
} = Astro.props;
---

<a class:list={[linkClass, "cta", "button"]} title={title} href={link}>
  {text}
</a>
